# Discord Bot — copy to .env (dev) or .env.prod (production). Do not commit real values.

# Bot token from https://discord.com/developers/applications → Your App → Bot
DISCORD_TOKEN=

# Application (client) ID from the same page under "Application ID"
DISCORD_CLIENTID=
